const BASE_SIZE = 10;

window.config = {
  size:BASE_SIZE ,
  speed: BASE_SIZE/200,
  //speed: 0,
  fps:30,
  gateSpeed : 0.01,
  steerReleaseFactor: 0.95,
  horizLimit:0.02,
  vertLimit:0.005,
  rotUnit:0.001,
};